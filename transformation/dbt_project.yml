 
name: 'transformation'
version: '1.0.0'

profile: 'transformation'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:   
  - "target"
  - "dbt_packages"
  - "logs"

models:
  transformation:
    materialized: view
